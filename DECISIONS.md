# 결정 기록 (ADR 스타일)

중요한 설계·기술 선택은 “왜 그렇게 했는지”를 남겨 두면, 나중에 같은 논의를 반복하지 않을 수 있습니다.

## 형식 예시

### ADR-000: 제목

- **상태:** 제안됨 / 수락됨 / 대체됨 / 폐기됨
- **맥락:** 결정이 필요했던 상황
- **결정:** 선택한 방안
- **결과:** 기대 효과와 트레이드오프

---

### ADR-001: Telegram Webhook + GAS + Calendar API v3

- **상태:** 수락됨
- **맥락:** 텔레그램에서 한국어 한 줄로 일정을 올리고, 조회·매일 요약까지 서버 비용 없이 운영하려는 목표.
- **결정:** Telegram `setWebhook`을 **Google Apps Script 웹앱** `doPost`에 연결하고, 일정 읽기/쓰기는 **고급 서비스 Calendar v3**(`Calendar.Events`)로 처리한다. Webhook URL 검증은 선택적 **`?key=`** 쿼리(스크립트 속성과 일치)로 처리한다(표준 `X-Telegram-Bot-Api-Secret-Token` 헤더는 GAS 웹앱에서 불안정할 수 있음).
- **결과:** 별도 Node 서버 없이 배포 가능. 트리거는 프로젝트 시간대 **Asia/Seoul** 및 `installDailyTrigger`로 고정. 반복·정밀 리마인더는 API 제약이 있어 종일/기간 일정은 필요 시 **짧은 `[알림]` 일정**을 보조로 삽입한다.
